buildscript {
   ext.kotlin_version = '1.8.0' // Updated Kotlin version
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
       // classpath "com.android.rg.jetbrainstools.build:gradle:8.1.1" // Updated Gradle plugin version
       // classpath "o.kotlin:kotlin-gradle-plugin:$kotlin_version"

        // Other dependencies
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'

subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
